apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Deploy Infisical operator first
  - infisical-operator.yaml
  # Then deploy auth credentials
  - infisical-auth.yaml

# Ensure proper deployment order
configurations:
  - kustomizeconfig.yaml
